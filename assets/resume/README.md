# assets/resume/

**The site links to `ian-solberg-resume.pdf` in this directory, and that file
does not exist yet.** Until it is added, the Resume link in the header, the
footer, and the two Resume buttons all return a 404.

Drop the PDF here with exactly this filename:

```
assets/resume/ian-solberg-resume.pdf
```

No other change is needed; nothing has to be edited to pick it up.

If a different filename is preferred, grep the site for
`ian-solberg-resume.pdf` and change every occurrence (it appears in the header
nav and footer of every page).

Ian, 2026-08-13: the resume "used to be on my previous profile" and "needs a
little touching up at some point", so the file that goes here may be a revision
rather than the existing one.
